require 'spec_helper'

describe User do
  it "must have a name"
  it "must have a password"
  it "must have an email"
  it "must have email confirmation"
  it "must have password confirmation"
  it "password must be at least 8 characters"
  it "must have a valid email"
end
